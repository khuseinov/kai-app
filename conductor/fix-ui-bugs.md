# План: Исправление бага с двойной иконкой загрузки и багов VoiceState

## Цель
1. Убрать "двойную" иконку загрузки (одна в ответе, другая внизу) и оставить только одну (внутри чата).
2. Исправить логику появления свечения (Gemini Wave) так, чтобы оно не конфликтовало с текстовым вводом.

## Архитектура
1. **ChatScreen**: Уберем `KaiCognitiveStatus` из AppBar, оставим только волну внизу (которая реагирует на состояние).
2. **ChatLoadingIndicator**: Оставим его как основной индикатор "думающего" Kai в ленте.
3. **Логика**: При тапе по экрану — только голос. При отправке текста — только лента.

---

### Задача 1: Чистка ChatScreen
- [ ] Шаг 1: Удалить `KaiCognitiveStatus` из AppBar в `ChatScreen.dart`.
- [ ] Шаг 2: Убедиться, что `_voiceStateFromChat` правильно переключает состояния.

### Задача 2: Оптимизация ленты (MessageList)
- [ ] Шаг 1: Убрать дубликаты индикаторов загрузки в `MessageList`.

### Задача 3: Коммит
```bash
git add .
git commit -m "fix: remove duplicate loading indicators and polish voice state ui"
git push origin master
```