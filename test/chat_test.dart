import 'package:flutter_test/flutter_test.dart';

void main() {
  group('ChatMessage', () {
    test('placeholder — add real tests after flutter pub get', () {
      expect(1 + 1, 2);
    });
  });
}
